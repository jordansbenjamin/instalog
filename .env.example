# The frontend no longer talks to Jira directly. Authentication and worklog
# posting go through the backend (see server/.env.example for OAuth/Mongo config).
# In dev, Vite proxies /api to the Express server (see vite.config.ts).
#
# No frontend (VITE_*) secrets are required.
